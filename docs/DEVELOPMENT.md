# PegaQuant 开发总览

## 1. 项目目标

- 交易主线：基于震荡结构识别（X/Y 锚点）进行趋势判断与交易执行。
- 当前重点：先完成可控可观测的交易接入（P1），再进入半自动化（P2）。
- 技术栈：Python 3.10 + FastAPI + SQLAlchemy + CCXT + Next.js。
  
## 2. 数据表结构在 models.py 中定义
编写一个初始化所有数据表的脚本，用于在项目启动时创建数据库表。
```

从 1m,5m,15m,30m,1h,4h 周期K线表中识别震荡结构

XY关键点，X下轨，Y上轨，规则如下：
1、XY关键点识别：必须是上下轨之外的关键点，取极值的那个作为临时点，当后续k线连续收盘价格都在30根以上都在轨内，就可以确定这个点为XY关键点。
2、如果价格(注意是价格，X这边取最低价格，Y这边取最高价格)低于前一个关键点5% 则表示震荡结构被破坏，关闭这个结构体，结束时间就是打破震荡结构的这根k线的open_time。这根k线也用于下一个结构开始计算关键点的起始位置，这样保证不会断。

震荡结构破坏，还需要考虑价格有没有突破当前结构中所有同侧关键点的极值，也就是最高或者最低那个关键点
请理解含义并回复
一个震荡结构结束的原因，必定是在某根K线 突破了5%设定的参数，这才是这个震荡结构结束的时间点和价格

